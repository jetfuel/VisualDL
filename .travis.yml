language: cpp
cache:
  - pip
  - ccache
  - yarn
  - npm
sudo: required
dist: trusty

os: linux

addons:
  apt:
    packages:
      - python
      - python-pip
      - python2.7-dev
      - python-wheel
      - python3-pip
      - python3-dev
      - python3-wheel
      - clang-format-3.8

script:
  - ./scripts/tests.sh all
