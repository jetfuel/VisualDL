language: cpp

compiler: clang

cache:
  - pip
  - ccache
  - yarn
  - npm

sudo: required

dist: trusty

os: linux

addons:
  apt:
    packages:
      - clang-3.7
      - gcc-4.8
      - g++-4.8
      - git
      - python
      - python-pip
      - python2.7-dev
      - python-wheel
      - python3-pip
      - python3-dev
      - python3-wheel
      - clang-format-3.8
      - ccache
      - npm
      - nodejs

script:
  - ./scripts/tests.sh all
